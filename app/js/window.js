
// Set window size
var width = 400
var height = 300

// Resize window
window.resizeTo(width, height);

// Center window
window.moveTo(
    (screen.width - width) / 2, 
    (screen.height - height) / 2
);